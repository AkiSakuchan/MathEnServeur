syntax = "proto3";

package commutationJsPhp;

service RPC
{
    rpc render(source) returns (result);
}

message source
{
    bool display = 1;
    string tex = 2;
}

message result
{
    string html = 1;
}